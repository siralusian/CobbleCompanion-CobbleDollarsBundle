/**
 * Fremd-Mod-Integration für Create (Lagerticker-Bezahlsystem) - Unterpackage der
 * com.cobblecompanion.integrations-Isolationsgrenze, siehe deren package-info.java.
 *
 * Unterpaket "mixin" enthält die einzigen Stellen, die per Bytecode-Eingriff (Sponge Mixin)
 * direkt in Creates eigene Klassen greifen (siehe cobblecompanion.mixins.json,
 * requiredMods=["create"] - lädt nur, wenn Create installiert ist).
 */
package com.cobblecompanion.integrations.create;
